# UI Prefab 节点蓝图输出模板

## 1. 分析摘要

- Prefab 类型：
- 视觉参考尺寸：
- 工程设计分辨率：
- 根节点尺寸与适配方式：
- 主要功能区域：
- 动态数据：
- 交互与滚动：
- 证据不足项：

## 2. 结构证据

| 证据来源 | 与目标相同的职责 | 采用的结构约定 | 不采用的差异 | 依据级别 |
| --- | --- | --- | --- | --- |
| 用户提示或效果图 | - | - | - | 确定/测量 |
| `db://assets/.../Reference.prefab` | - | - | - | 工程事实 |
| `assets/.../Controller.ts` | - | - | - | 代码接口 |

只列 1 至 3 个最接近的参考 Prefab。不要用全工程中最复杂的界面填充目标结构。

## 3. Prefab 节点树

树表达层级、渲染顺序、组件概览和节点来源：

```text
FeaturePanel.prefab
└── FeaturePanel [UITransform, ReferenceCollector, Controller] {local}
    ├── CloseOverlay [UITransform, MButton] {local}
    └── home [UITransform] {local}
        ├── Background [UITransform, Sprite] {local}
        ├── TopCurrency [Prefab] {instance: db://assets/.../topGold.prefab}
        ├── Content [UITransform, Layout] {local}
        │   └── ItemTemplate [UITransform, ItemController] {template, inactive}
        └── EffectMount {runtime}
```

来源标签只使用：

- `{local}`：当前 Prefab 的本地序列化节点。
- `{instance: db://...}`：嵌套 Prefab 实例，不展开内部节点。
- `{template}`：运行时克隆的序列化模板。
- `{runtime}`：脚本运行时创建，Prefab 编辑阶段只存在挂点。

所有本地 UI 节点默认包含 UITransform。为可读性在树中省略时，必须在规格表中列出。

## 4. 控制器、引用与复用

### 控制器和引用作用域

| 作用域根路径 | 控制组件 | 引用收集器 | 引用节点路径 | 脚本 key | 按钮归属规则 | 依据 |
| --- | --- | --- | --- | --- | --- | --- |

每个引用节点只归属一个作用域。嵌套收集器形成新边界，外层作用域不得列出其内部引用。

### Prefab 实例

| 实例节点路径 | Prefab URL | 用途 | 属性覆盖 | 是否允许改源 Prefab | 依据 |
| --- | --- | --- | --- | --- | --- |

不复制公共 Prefab 的内部节点树。若必须引用内部挂点，记录公开路径或脚本接口，不把它伪装成本地节点。

### 路径和排序约束

| 节点路径 | AnimationClip/脚本路径引用 | 排序边界 | 允许改名或换父节点 | 同步动作 |
| --- | --- | --- | --- | --- |

## 5. 关键节点规格表

| 节点完整路径 | 来源 | 用途 | 尺寸 W×H | 本地位置 X,Y | 锚点 X,Y | 组件与关键属性 | 默认显示 | 动态 | 点击 | 独立动画 | 依据 |
| --- | --- | --- | ---: | ---: | ---: | --- | --- | --- | --- | --- | --- |
| FeaturePanel | local | 根节点 | 设计分辨率 | 0,0 | 0.5,0.5 | UITransform | 是 | 否 | 否 | 否 | 确定 |

“依据”只使用：

- `确定`：来自用户明确值。
- `工程事实`：来自 Prefab、脚本、项目设置或资源。
- `测量`：直接从效果图测得。
- `估算`：不能精确测量的合理近似。
- `待确认`：现有证据无法决定。

每个路径必须唯一。位置始终相对于父节点。实例内部节点不在本表重复定义。

## 6. 重复内容与状态

### 重复内容

| 容器路径 | 类型 | Prefab 内数量 | 运行时数量 | item 来源 | 模板默认显示 | 脚本访问方式 | 依据 |
| --- | --- | ---: | --- | --- | --- | --- | --- |

类型填写固定槽位、状态子节点、动态模板、设计样本或运行时生成。

### 界面状态

| 状态 | 显示节点 | 隐藏节点 | 属性变化 | 默认 | 互斥组 | 控制者 | 依据 |
| --- | --- | --- | --- | --- | --- | --- | --- |

项目 Switch 另列其直接子节点顺序和默认 `checkIndex`。不要将 Switch 状态子节点误写成可删除的重复副本。

## 7. 视觉与交互配置

### Sprite

| 节点路径 | 资源或占位模式 | Type | SizeMode | 保持比例 | 九宫格边距 | 裁剪/填充 | 动态/状态用途 | 正式资源几何策略 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |

资源存在时填写 `db://` URL。只有对应资源缺失时填写“纯色占位”，并在第 10 节列出待补资源；同时记录最终节点应保持既有几何、采用素材几何还是使用九宫格适配，供后续换装直接执行。

### Label

| 节点路径 | 文字/占位 | 字号 | 行高 | 颜色 | 水平/垂直对齐 | Overflow | 描边 | 阴影 | 自动缩小 | 多行 | 动态 |
| --- | --- | ---: | ---: | --- | --- | --- | --- | --- | --- | --- | --- |

### 交互与复合控件

| 根节点路径 | 类型 | 热区尺寸 | Transition | 状态视觉 | 引用关系 | 事件归属 | 阻止穿透 | 备注 |
| --- | --- | ---: | --- | --- | --- | --- | --- | --- |

- MButton 或 Button 说明 normal、pressed、disabled 和稳定按钮名。
- Switch 说明直接子节点顺序、名称和 `checkIndex`。
- Toggle 说明 ToggleContainer、默认项、`allowSwitchOff` 和 checkMark。
- ScrollView 说明 viewport、Mask、Content、方向、惯性、回弹和 `viewportSource`。

## 8. Widget 与适配

| 节点路径 | Top | Bottom | Left | Right | HCenter | VCenter | 主要控制者 | 适配说明 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | --- | --- |

未启用的值填 `-`。补充说明背景裁剪、固定尺寸元素、允许伸缩区域和安全区策略。

## 9. 拼装顺序

按依赖分阶段列出：

1. 创建 Prefab 根节点、根控制组件和引用收集器。
2. 实例化公共 Prefab，设置实例根位置和允许的属性覆盖。
3. 创建本地功能容器、固定槽位和模板挂点。
4. 创建叶子视觉、状态与交互节点。
5. 添加组件并设置尺寸、锚点、激活状态和基础属性。
6. 绑定 Mask、ScrollView、ProgressBar、Switch、Toggle 等引用。
7. 刷新引用收集器，从内向外更新 Layout 和 Widget。
8. 检查按钮归属、状态子节点顺序、动画路径、排序边界、渲染顺序和动态模板。
9. 保存、比较实际树与蓝图并运行 UI 校验。

## 10. 待确认、估算与占位项

| 项目/节点 UUID | 类型 | 当前处理或资源 | 预期正式资源 | 几何策略 | 状态/动态用途 | 影响范围 | 确认后动作 |
| --- | --- | --- | --- | --- | --- | --- | --- |

类型填写待确认、估算或纯色资源占位。集中列出所有不确定项，不把它们混入工程事实。
